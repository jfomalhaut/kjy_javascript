import React from 'react';

const P1 = ({history, match}) => {
    // console.log(match.params.id);
    return (
        <div>
            page3/p1
            {/* <div>this page is {id}</div>
            <button onClick={()=> history.goBack()}>back</button> */}
        </div>
    );
}

export default P1;